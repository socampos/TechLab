---
- name: Backup TEDs
  hosts: TED
  gather_facts: yes
  collections:
   - fortinet.fortios
   
  connection: httpapi
  vars:
   vdom: "root"
   ansible_httpapi_use_ssl: yes
   ansible_httpapi_validate_certs: no
   ansible_httpapi_port: 443
   
  tasks:
  - name: Backup?
    fortios_monitor_fact:
     vdom: "{{ vdom }}"
     selector: 'system_interface'
     register: backupinfo

  - debug:
    var: backupinfo.stdout_lines
