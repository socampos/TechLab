---
- name: Playbook to handle users
  hosts: Lab
  become: true

  tasks:
  # Ensure user pinky is present
  - ipauser:
      ipaadmin_password: techadmin1234
      name: jorge
      first: Jorge
      last: Maidana
